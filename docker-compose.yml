version: "3"
services:
  authentication:
    build: ./authentication
    volumes:
      - ./authentication:/usr/src/app
      - /usr/src/app/node_modules
    ports:
      - 6010:6010
  hello:
    build: ./hello
    volumes:
      - ./hello:/usr/src/app
      - /usr/src/app/node_modules
    ports:
      - 6020:6020